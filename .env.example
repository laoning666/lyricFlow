# LyricFlow - Environment Configuration
# LyricFlow - 环境配置
#
# Copy this file to .env and modify as needed:
# 复制此文件为 .env 并根据需要修改：
#   cp .env.example .env

# ============================================
# Music Path Configuration / 音乐路径配置
# ============================================

# Path to music folder inside container (default: /music)
# 容器内音乐文件夹路径（默认：/music）
MUSIC_PATH=/music

# Scan interval in days / 扫描间隔（天）
# 0 = run once and exit / 运行一次后退出
# 1 = scan every day / 每天扫描
# 7 = scan every week / 每周扫描
SCAN_INTERVAL_DAYS=0

# ============================================
# Download Behavior / 下载行为
# ============================================

# Whether to download lyrics (.lrc files)
# 是否下载歌词（.lrc 文件）
DOWNLOAD_LYRICS=true

# Whether to download album covers (cover.jpg)
# 是否下载专辑封面（cover.jpg）
DOWNLOAD_COVER=true

# Whether to overwrite existing lyrics files
# 是否覆盖已存在的歌词文件
OVERWRITE_LYRICS=false

# Whether to overwrite existing cover files
# 是否覆盖已存在的封面文件
OVERWRITE_COVER=false

# ============================================
# Artist/Album Inference / 歌手/专辑推断
# ============================================

# Infer artist and album from folder structure
# 从文件夹结构推断歌手和专辑
# Expects: /music/Artist/Album/song.mp3
# 期望结构：/music/歌手/专辑/歌曲.mp3
# Set to false if your music is not organized this way
# 如果音乐未按此方式组织，请设为 false
USE_FOLDER_STRUCTURE=true

# Default artist (fallback when ID3 tag and folder inference both fail)
# 默认歌手（当 ID3 标签和文件夹推断都失败时使用）
# Leave empty to skip songs without artist info
# 留空则跳过无歌手信息的歌曲
# Example / 示例: DEFAULT_ARTIST=周杰伦
DEFAULT_ARTIST=

# ============================================
# Search Configuration / 搜索配置
# ============================================

# Platform priority for search (comma-separated)
# 搜索平台优先级（逗号分隔）
# Available / 可选值: netease, kuwo, qq
# The first platform with a match will be used
# 将使用第一个匹配的平台
PLATFORMS=netease,kuwo,qq

# ============================================
# API Configuration (Advanced) / API 配置（高级）
# ============================================

# TuneHub API base URL (usually no need to change)
# TuneHub API 基础 URL（通常无需修改）
API_BASE_URL=https://music-dl.sayqz.com
